{
    "version": "2.0.0",
    "tasks": [
        {
            "taskName": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/s2-ingestion/dotnet/dotnet.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}